#!/bin/sh
# Fast, no network: the local gate. env-guard/gitleaks live here, not in
# pre-push — a secret only caught at push time is already in local history.
set -e
./scripts/local-ci-checks.sh env-guard
./scripts/local-ci-checks.sh gitleaks
./scripts/verify.sh
